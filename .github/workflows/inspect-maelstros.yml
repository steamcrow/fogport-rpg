name: Inspect MAELSTROS — Read Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect-maelstros:
    name: Read MAELSTROS location hierarchy
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install --requirement requirements.txt

      - name: Inspect MAELSTROS only
        env:
          KANKA_API_TOKEN: ${{ secrets.KANKA_API_TOKEN }}
        run: python scripts/inspect_maelstros.py
