[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "fogport-kanka-librarian"
version = "0.1.0"
description = "Approval-gated Kanka publishing tools for Fogport"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["kanka_librarian"]
